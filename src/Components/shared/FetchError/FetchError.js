import React from 'react';
import styles from './FetchError.scss';

const FetchError = () => (
  <div className={styles.shadowBlockGlobal}>
    <h2>Error done</h2>
  </div>
);

export default FetchError;
